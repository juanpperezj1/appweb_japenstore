import { ContraseniaDPipe } from './contrasenia-d.pipe';

describe('ContraseniaDPipe', () => {
  it('create an instance', () => {
    const pipe = new ContraseniaDPipe();
    expect(pipe).toBeTruthy();
  });
});
